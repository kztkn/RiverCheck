{
  "name": "rivercheck",
  "private": true,
  "type": "module",
  "engines": {
    "node": ">=22.22.0"
  },
  "scripts": {
    "dev": "react-router dev",
    "build": "npm run license:check && npm run pwa:icons && react-router build && npm run pwa:build",
    "release:build": "npm run typecheck && npm test && npm run build && npm run db:migrate:production",
    "preview": "npm run build && vite preview",
    "deploy": "npm run build && wrangler deploy",
    "typecheck": "react-router typegen && tsc -b",
    "test": "vitest run",
    "test:watch": "vitest",
    "license:check": "node scripts/check-production-licenses.mjs",
    "pwa:build": "node scripts/build-pwa.mjs",
    "pwa:icons": "node scripts/generate-pwa-icons.mjs",
    "db:migrate": "npm run db:migrate:local",
    "db:seed": "npm run db:seed:local",
    "db:migrate:local": "DATABASE_URL=postgresql://postgres:postgres@localhost:5432/rivercheck TMPDIR=${TMPDIR:-/tmp} tsx server/db/migrate.ts",
    "db:seed:local": "DATABASE_URL=postgresql://postgres:postgres@localhost:5432/rivercheck TMPDIR=${TMPDIR:-/tmp} tsx server/db/seed.ts",
    "db:migrate:production": "TMPDIR=${TMPDIR:-/tmp} tsx server/db/migrate.ts",
    "db:seed:production": "TMPDIR=${TMPDIR:-/tmp} tsx server/db/seed.ts",
    "cf-typegen": "wrangler types"
  },
  "dependencies": {
    "@pushforge/builder": "2.0.5",
    "@tabler/icons-react": "^3.46.0",
    "isbot": "^5",
    "pg": "^8.16.3",
    "qrcode.react": "^4.2.0",
    "react": "^19.2.7",
    "react-dom": "^19.2.7",
    "react-router": "^8.3.0",
    "recharts": "^3.10.1"
  },
  "devDependencies": {
    "@cloudflare/vite-plugin": "^1.29.1",
    "@cloudflare/workers-types": "^5.20260804.1",
    "@react-router/dev": "^8.3.0",
    "@types/node": "^24.10.1",
    "@types/pg": "^8.15.6",
    "@types/react": "^19.2.14",
    "@types/react-dom": "^19.2.3",
    "dotenv": "^17.2.3",
    "tsx": "^4.20.6",
    "typescript": "^5.9.3",
    "vite": "^8.0.3",
    "vitest": "^4.0.18",
    "wrangler": "^4.75.0"
  }
}
