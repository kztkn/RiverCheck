name: achievement-refresh-reliability

on:
  push:
    branches:
      - chatgpt/achievement-refresh-reliability-20260913

permissions:
  contents: write

jobs:
  implement-and-validate:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          ref: chatgpt/achievement-refresh-reliability-20260913
          fetch-depth: 0
      - uses: actions/setup-node@v4
        with:
          node-version: 24
          cache: npm
      - run: npm ci
      - name: Apply implementation
        shell: bash
        run: |
          python3 <<'PY'
          from pathlib import Path

          def replace_once(path, old, new):
              p = Path(path)
              text = p.read_text()
              if old not in text:
                  raise SystemExit(f"expected text not found in {path}: {old[:120]!r}")
              p.write_text(text.replace(old, new, 1))

          repo = Path("server/repositories/achievement-repository.server.ts")
          text = repo.read_text()
          text = text.replace(
              "  is_equipped: boolean;\n}",
              "  is_equipped: boolean;\n  achievements_dirty: boolean;\n}",
              1,
          )
          text = text.replace(
              "): Promise<void> {\n  if (playerUnlocks.length === 0) return;\n  await transaction.query(\n",
              "): Promise<number> {\n  if (playerUnlocks.length === 0) return 0;\n  const result = await transaction.query(\n",
              1,
          )
          text = text.replace(
              "    ],\n  );\n}\n\nexport async function listPlayerAchievementCollection(\n",
              "    ],\n  );\n  return result.rowCount ?? 0;\n}\n\nexport async function markAchievementRefreshNeeded(\n  transaction: DatabaseTransaction,\n  groupId: string,\n  groupPlayerIds: string[],\n): Promise<void> {\n  const uniquePlayerIds = [...new Set(groupPlayerIds)];\n  if (uniquePlayerIds.length === 0) return;\n  await transaction.query(\n    `\n      UPDATE group_players\n      SET achievements_dirty = TRUE\n      WHERE group_id = $1\n        AND id = ANY($2::UUID[])\n    `,\n    [groupId, uniquePlayerIds],\n  );\n}\n\nexport async function lockAchievementRefreshTargets(\n  transaction: DatabaseTransaction,\n  groupId: string,\n  groupPlayerIds: string[],\n): Promise<string[]> {\n  const uniquePlayerIds = [...new Set(groupPlayerIds)];\n  if (uniquePlayerIds.length === 0) return [];\n  const result = await transaction.query<{ id: string }>(\n    `\n      SELECT id\n      FROM group_players\n      WHERE group_id = $1\n        AND id = ANY($2::UUID[])\n      ORDER BY id\n      FOR UPDATE\n    `,\n    [groupId, uniquePlayerIds],\n  );\n  return result.rows.map((row) => row.id);\n}\n\nexport async function clearAchievementRefreshNeeded(\n  transaction: DatabaseTransaction,\n  groupId: string,\n  groupPlayerIds: string[],\n): Promise<void> {\n  if (groupPlayerIds.length === 0) return;\n  await transaction.query(\n    `\n      UPDATE group_players\n      SET achievements_dirty = FALSE\n      WHERE group_id = $1\n        AND id = ANY($2::UUID[])\n    `,\n    [groupId, groupPlayerIds],\n  );\n}\n\nexport async function listPlayerAchievementCollection(\n",
              1,
          )
          text = text.replace(
              "): Promise<PlayerAchievementCollection> {\n",
              "): Promise<PlayerAchievementCollection & { needsRefresh: boolean }> {\n",
              1,
          )
          text = text.replace(
              "        SELECT id, equipped_achievement_id\n",
              "        SELECT id, equipped_achievement_id, achievements_dirty\n",
              1,
          )
          text = text.replace(
              "        achievement.is_hidden,\n        player_achievement.unlocked_at,\n",
              "        achievement.is_hidden,\n        target_player.achievements_dirty,\n        player_achievement.unlocked_at,\n",
              1,
          )
          text = text.replace(
              "  return {\n    unlockedCount: items.filter((item) => item.isUnlocked).length,\n",
              "  return {\n    needsRefresh: result.rows[0]?.achievements_dirty ?? false,\n    unlockedCount: items.filter((item) => item.isUnlocked).length,\n",
              1,
          )
          repo.write_text(text)

          Path("server/services/achievement-service.server.ts").write_text('''import { waitUntil } from "cloudflare:workers";
import { evaluateAchievements } from "@domain/achievement/evaluate-achievements";
import {
  clearAchievementRefreshNeeded,
  insertAchievementUnlocks,
  listAchievementHistoryGames,
  listAchievementReactionSummaries,
  listPendingAchievementNotifications,
  listPlayerAchievementCollection,
  listUnlockedAchievementIds,
  lockAchievementRefreshTargets,
  markAchievementNotificationSeen,
  markAchievementRefreshNeeded,
} from "@server/repositories/achievement-repository.server";
import {
  withTransaction,
  type DatabaseTransaction,
} from "@server/db/client.server";
import type {
  PendingAchievementNotification,
  PlayerAchievementCollection,
} from "@shared-types/achievement";

interface AchievementRefreshContext {
  reason: string;
  gameId?: string;
}

export async function awardAchievementsForPlayers(
  transaction: DatabaseTransaction,
  groupId: string,
  groupPlayerIds: string[],
): Promise<number> {
  const uniquePlayerIds = [...new Set(groupPlayerIds)];
  if (uniquePlayerIds.length === 0) return 0;

  const history = await listAchievementHistoryGames(
    transaction,
    groupId,
    uniquePlayerIds,
  );
  const reactionSummaries = await listAchievementReactionSummaries(
    transaction,
    groupId,
    uniquePlayerIds,
  );
  const reactionByPlayer = new Map(
    reactionSummaries.map((summary) => [summary.groupPlayerId, summary]),
  );

  const playerUnlocks = uniquePlayerIds.flatMap((groupPlayerId) => {
    const games = history
      .filter((game) => game.groupPlayerId === groupPlayerId)
      .map((game) => ({
        gameId: game.gameId,
        rank: game.rank,
        participantCount: game.participantCount,
        netBb: game.netBb,
        totalRebuyCount: game.totalRebuyCount,
        outstandingRebuyCount: game.outstandingRebuyCount,
        settlementRebuyCount: game.settlementRebuyCount,
        sevenDeuceCount: game.sevenDeuceCount,
        allInWinCount: game.allInWinCount,
        allInLossCount: game.allInLossCount,
        storyPostCount: game.storyPostCount,
      }));
    const reaction = reactionByPlayer.get(groupPlayerId);
    return evaluateAchievements(games, {
      reactedStoryPostCount: reaction?.reactedStoryPostCount ?? 0,
      fifthReactedStoryGameId: reaction?.fifthReactedStoryGameId ?? null,
    }).map((unlock) => ({ groupPlayerId, unlock }));
  });

  return insertAchievementUnlocks(transaction, groupId, playerUnlocks);
}

export async function refreshAchievementsForPlayers(
  groupId: string,
  groupPlayerIds: string[],
): Promise<number> {
  const uniquePlayerIds = [...new Set(groupPlayerIds)];
  if (uniquePlayerIds.length === 0) return 0;

  return withTransaction(async (transaction) => {
    // Lock the player rows before evaluating history. If a newer mutation tries
    // to mark the same player dirty, it waits and leaves dirty=true afterwards.
    const targets = await lockAchievementRefreshTargets(
      transaction,
      groupId,
      uniquePlayerIds,
    );
    if (targets.length === 0) return 0;
    const newUnlockCount = await awardAchievementsForPlayers(
      transaction,
      groupId,
      targets,
    );
    await clearAchievementRefreshNeeded(transaction, groupId, targets);
    return newUnlockCount;
  });
}

export async function markAchievementsDirtyBestEffort(
  groupId: string,
  groupPlayerIds: string[],
  context: AchievementRefreshContext,
): Promise<boolean> {
  const uniquePlayerIds = [...new Set(groupPlayerIds)];
  if (uniquePlayerIds.length === 0) return true;
  try {
    await withTransaction((transaction) =>
      markAchievementRefreshNeeded(transaction, groupId, uniquePlayerIds)
    );
    return true;
  } catch (error) {
    console.error("Failed to mark achievements for refresh", {
      errorType: error instanceof Error ? error.name : "unknown",
      gameId: context.gameId,
      playerCount: uniquePlayerIds.length,
      reason: context.reason,
    });
    return false;
  }
}

export async function refreshAchievementsBestEffort(
  groupId: string,
  groupPlayerIds: string[],
  context: AchievementRefreshContext,
): Promise<boolean> {
  const uniquePlayerIds = [...new Set(groupPlayerIds)];
  if (uniquePlayerIds.length === 0) return true;
  const startedAt = Date.now();
  console.info("Achievement refresh started", {
    gameId: context.gameId,
    playerCount: uniquePlayerIds.length,
    reason: context.reason,
  });
  try {
    const newUnlockCount = await refreshAchievementsForPlayers(
      groupId,
      uniquePlayerIds,
    );
    console.info("Achievement refresh succeeded", {
      durationMs: Date.now() - startedAt,
      gameId: context.gameId,
      newUnlockCount,
      playerCount: uniquePlayerIds.length,
      reason: context.reason,
    });
    return true;
  } catch (error) {
    console.error("Achievement refresh failed", {
      durationMs: Date.now() - startedAt,
      errorType: error instanceof Error ? error.name : "unknown",
      gameId: context.gameId,
      playerCount: uniquePlayerIds.length,
      reason: context.reason,
    });
    return false;
  }
}

export function scheduleAchievementRefresh(
  groupId: string,
  groupPlayerIds: string[],
  context: AchievementRefreshContext,
): void {
  const uniquePlayerIds = [...new Set(groupPlayerIds)];
  if (uniquePlayerIds.length === 0) return;

  waitUntil((async () => {
    await markAchievementsDirtyBestEffort(groupId, uniquePlayerIds, context);
    await refreshAchievementsBestEffort(groupId, uniquePlayerIds, context);
  })());
}

export async function getPlayerAchievementCollection(
  groupId: string,
  groupPlayerId: string,
): Promise<PlayerAchievementCollection> {
  let collection = await listPlayerAchievementCollection(groupId, groupPlayerId);
  if (collection.needsRefresh) {
    const refreshed = await refreshAchievementsBestEffort(
      groupId,
      [groupPlayerId],
      { reason: "collection-repair" },
    );
    if (refreshed) {
      collection = await listPlayerAchievementCollection(groupId, groupPlayerId);
    }
  }
  const { needsRefresh: _needsRefresh, ...publicCollection } = collection;
  return {
    ...publicCollection,
    items: publicCollection.items.map((achievement) =>
      achievement.isHidden && !achievement.isUnlocked
        ? {
            ...achievement,
            name: "???",
            description: "条件は秘密",
          }
        : achievement
    ),
  };
}

export async function getUnlockedPlayerAchievementIds(
  groupPlayerId: string,
): Promise<string[]> {
  return listUnlockedAchievementIds(groupPlayerId);
}

export async function getPendingPlayerAchievementNotifications(
  groupId: string,
  groupPlayerId: string,
): Promise<PendingAchievementNotification[]> {
  return listPendingAchievementNotifications(groupId, groupPlayerId);
}

export async function acknowledgePlayerAchievementNotification(
  groupId: string,
  groupPlayerId: string,
  playerAchievementId: string,
): Promise<boolean> {
  return markAchievementNotificationSeen(
    groupId,
    groupPlayerId,
    playerAchievementId,
  );
}
''')

          finalization = Path("server/services/finalization-service.server.ts")
          text = finalization.read_text()
          text = text.replace(
              'import {\n  awardAchievementsForPlayers,\n  scheduleAchievementRefresh,\n} from "@server/services/achievement-service.server";',
              'import {\n  awardAchievementsForPlayers,\n  markAchievementsDirtyBestEffort,\n  refreshAchievementsBestEffort,\n} from "@server/services/achievement-service.server";',
              1,
          )
          text = text.replace(
              '''  scheduleAchievementRefresh(group.id, finalized.achievementPlayerIds, {\n    reason: "finalization",\n    gameId,\n  });\n\n  try {''',
              '''  // The finalized result is already committed here. Achievement work may\n  // delay this response briefly, but it must never turn a valid result into a\n  // failed finalization. Dirty state lets a later collection view self-repair.\n  await markAchievementsDirtyBestEffort(\n    group.id,\n    finalized.achievementPlayerIds,\n    { reason: "finalization", gameId },\n  );\n  await refreshAchievementsBestEffort(\n    group.id,\n    finalized.achievementPlayerIds,\n    { reason: "finalization", gameId },\n  );\n\n  try {''',
              1,
          )
          finalization.write_text(text)

          Path("migrations/0030_track_achievement_refresh.sql").write_text('''ALTER TABLE group_players\nADD COLUMN achievements_dirty BOOLEAN NOT NULL DEFAULT FALSE;\n\n-- Repair any unlocks missed by the previous best-effort background refresh.\n-- The next achievement collection read recalculates these players once, then\n-- clears the flag. New finalizations mark the same flag before recalculation.\nUPDATE group_players AS group_player\nSET achievements_dirty = TRUE\nWHERE EXISTS (\n  SELECT 1\n  FROM game_results AS game_result\n  INNER JOIN games AS game ON game.id = game_result.game_id\n  WHERE game_result.group_player_id = group_player.id\n    AND game.group_id = group_player.group_id\n    AND game.status = 'finalized'\n);\n''')

          test = Path("app/routes/game-finalized-notification.test.ts")
          text = test.read_text()
          text = text.replace(
              "  notifyGameFinalized: vi.fn(),\n  scheduleAchievements: vi.fn(),\n  saveCostSettings: vi.fn(),",
              "  markAchievementsDirty: vi.fn(),\n  notifyGameFinalized: vi.fn(),\n  refreshAchievements: vi.fn(),\n  saveCostSettings: vi.fn(),",
              1,
          )
          text = text.replace(
              '''vi.mock("@server/services/achievement-service.server", () => ({\n  awardAchievementsForPlayers: vi.fn(),\n  scheduleAchievementRefresh: mocked.scheduleAchievements,\n}));''',
              '''vi.mock("@server/services/achievement-service.server", () => ({\n  awardAchievementsForPlayers: vi.fn(),\n  markAchievementsDirtyBestEffort: mocked.markAchievementsDirty,\n  refreshAchievementsBestEffort: mocked.refreshAchievements,\n}));''',
              1,
          )
          text = text.replace(
              '''    mocked.scheduleAchievements.mockImplementation(() => {\n      mocked.events.push("achievements");\n    });''',
              '''    mocked.markAchievementsDirty.mockImplementation(async () => {\n      mocked.events.push("achievements-dirty");\n      return true;\n    });\n    mocked.refreshAchievements.mockImplementation(async () => {\n      mocked.events.push("achievements");\n      return true;\n    });''',
              1,
          )
          text = text.replace(
              'expect(mocked.events).toEqual(["committed", "achievements", "notified"]);',
              'expect(mocked.events).toEqual([\n      "committed",\n      "achievements-dirty",\n      "achievements",\n      "notified",\n    ]);',
              1,
          )
          marker = '''  it("通知失敗でも確定結果を成功として返す", async () => {'''
          addition = '''  it("称号更新失敗でも確定結果を成功として返す", async () => {\n    mocked.refreshAchievements.mockImplementation(async () => {\n      mocked.events.push("achievements-failed");\n      return false;\n    });\n\n    await expect(\n      finalizeGame(group, gameId, settings, false, false),\n    ).resolves.toEqual({ ok: true });\n    expect(mocked.events).toEqual([\n      "committed",\n      "achievements-dirty",\n      "achievements-failed",\n      "notified",\n    ]);\n  });\n\n'''
          if marker not in text:
              raise SystemExit("notification test marker not found")
          text = text.replace(marker, addition + marker, 1)
          test.write_text(text)

          repo_test = Path("app/routes/achievement-repository.test.ts")
          text = repo_test.read_text()
          text = text.replace(
              '''  insertAchievementUnlocks,\n  listAchievementHistoryGames,\n  listAchievementReactionSummaries,''',
              '''  clearAchievementRefreshNeeded,\n  insertAchievementUnlocks,\n  listAchievementHistoryGames,\n  listAchievementReactionSummaries,\n  lockAchievementRefreshTargets,\n  markAchievementRefreshNeeded,''',
              1,
          )
          marker = '''  it("loads finalized result, table-event, and story facts in one history query", async () => {'''
          addition = '''  it("marks, locks, and clears achievement refresh state", async () => {\n    const query = vi.fn()\n      .mockResolvedValueOnce({ rows: [], rowCount: 1 })\n      .mockResolvedValueOnce({ rows: [{ id: "player-1" }], rowCount: 1 })\n      .mockResolvedValueOnce({ rows: [], rowCount: 1 });\n    const transaction = transactionWith(query);\n\n    await markAchievementRefreshNeeded(transaction, "group-1", ["player-1"]);\n    await expect(lockAchievementRefreshTargets(\n      transaction,\n      "group-1",\n      ["player-1"],\n    )).resolves.toEqual(["player-1"]);\n    await clearAchievementRefreshNeeded(transaction, "group-1", ["player-1"]);\n\n    expect(String(query.mock.calls[0]?.[0])).toContain("achievements_dirty = TRUE");\n    expect(String(query.mock.calls[1]?.[0])).toContain("FOR UPDATE");\n    expect(String(query.mock.calls[2]?.[0])).toContain("achievements_dirty = FALSE");\n  });\n\n'''
          if marker not in text:
              raise SystemExit("achievement repository test marker not found")
          text = text.replace(marker, addition + marker, 1)
          repo_test.write_text(text)

          Path("app/routes/achievement-refresh-policy.test.ts").write_text('''import { describe, expect, it } from "vitest";\nimport { readFileSync } from "node:fs";\n\ndescribe("achievement refresh policy", () => {\n  it("keeps finalization authoritative and repairs only dirty collections", () => {\n    const finalization = readFileSync(\n      "server/services/finalization-service.server.ts",\n      "utf8",\n    );\n    const service = readFileSync(\n      "server/services/achievement-service.server.ts",\n      "utf8",\n    );\n    const migration = readFileSync(\n      "migrations/0030_track_achievement_refresh.sql",\n      "utf8",\n    );\n\n    expect(finalization).toContain("await refreshAchievementsBestEffort");\n    expect(finalization).not.toContain(\n      'scheduleAchievementRefresh(group.id, finalized.achievementPlayerIds',\n    );\n    expect(service).toContain("if (collection.needsRefresh)");\n    expect(service).toContain('reason: "collection-repair"');\n    expect(service).toContain('console.info("Achievement refresh succeeded"');\n    expect(migration).toContain("achievements_dirty BOOLEAN NOT NULL DEFAULT FALSE");\n    expect(migration).toContain("game.status = 'finalized'");\n  });\n});\n''')

          requirements = Path("docs/requirements.md")
          text = requirements.read_text()
          old = "- 結果確定ではgame_results保存とgameのfinalized化を先にtransactionでcommitし、称号再評価はCloudflare `waitUntil`へ登録してレスポンスを待たせない。判定は称号ごとにSQLを増やさず、確定履歴・テーブルイベント・STORIESをまとめた集約query、リアクション集約query、batch INSERTで行う"
          new = "- 結果確定ではgame_results保存とgameのfinalized化を先にtransactionでcommitすることを最優先とし、commit後に称号再評価を同期で1回待つ。称号更新の失敗は結果確定を失敗扱いにせず、対象プレイヤーの再評価フラグを残す。個人ページの称号コレクションは通常は読取りだけとし、再評価フラグが残っている場合だけ自己修復してフラグを解除する。称号再評価は開始・成功・失敗と処理時間・新規獲得件数をWorkerログへ残す。判定は称号ごとにSQLを増やさず、確定履歴・テーブルイベント・STORIESをまとめた集約query、リアクション集約query、batch INSERTで行う"
          if old not in text:
              raise SystemExit("requirements achievement policy not found")
          requirements.write_text(text.replace(old, new, 1))

          architecture = Path("docs/architecture.md")
          text = architecture.read_text()
          old = "service が `pg` の client を取得して `BEGIN` し、gameと参加者行をロックする。全員の入力と4人以上の参加を確認し、domain関数で検算、点数、順位、負担額を計算する。差分がある場合は主催者の確認を必須にする。game_resultsへのINSERT、gameのfinalized更新、現在の確定済み履歴に基づく実績同期を同一transactionで実行し、途中失敗時はrollbackする。"
          new = "service が `pg` の client を取得して `BEGIN` し、gameと参加者行をロックする。全員の入力と2人以上の参加を確認し、domain関数で検算、点数、順位、負担額を計算する。差分がある場合は主催者の確認を必須にする。game_resultsへのINSERTとgameのfinalized更新を同一transactionでcommitし、ここを結果確定の成功条件とする。commit後に対象playerの`achievements_dirty`をbest effortで立て、実績再評価を同期で1回待つ。実績再評価が失敗しても確定結果は成功のままとし、dirtyが残っていれば次回の称号コレクション読取り時だけ再評価して自己修復する。正常時の個人ページでは実績再計算を行わない。"
          if old not in text:
              raise SystemExit("architecture finalize policy not found")
          architecture.write_text(text.replace(old, new, 1))
          PY
      - run: git diff --check
      - run: npm run typecheck
      - run: npm test
      - run: npm run build
      - name: Commit validated changes and remove temporary workflow
        shell: bash
        run: |
          git rm .github/workflows/achievement-refresh-reliability.yml
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git add server/services/achievement-service.server.ts \
                  server/services/finalization-service.server.ts \
                  server/repositories/achievement-repository.server.ts \
                  migrations/0030_track_achievement_refresh.sql \
                  app/routes/achievement-repository.test.ts \
                  app/routes/achievement-refresh-policy.test.ts \
                  app/routes/game-finalized-notification.test.ts \
                  docs/requirements.md docs/architecture.md
          git commit -m "称号更新を確定後同期と自己修復に変更"
          git push origin HEAD:chatgpt/achievement-refresh-reliability-20260913
